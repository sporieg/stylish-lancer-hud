A pure types library for the lancer types.

Generally directly copied from the lancer repo by building just a types export then by copying .d.ts files manually as needed.

It may contain warnings, that does not really matter all that matters is it provides better type hints than `any`
